apiVersion: v1
kind: Service
metadata:
  name: blue-svc
spec:
  selector: 
    app: nginx-canary
  ports:
    - port: 80
      targetPort: 80
  type: NodePort

---
apiVersion: v1
kind: Service
metadata:
  name: green-svc
spec:
  selector:
    app: nginx-canary
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
